{
  "name": "errorfix-dev",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-pages.js",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "dev": "vite",
    "build": "node scripts/generate-pages.js && node scripts/generate-sitemap.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "google-trends-api": "^4.9.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
